# MCTS-Kalman inference, agency-only intervention, QCBF, velocity particles.
_base: defaults.yaml

inference:
  type: mcts_kalman

intervention:
  type: agency_only
  ref_controls: mcts-qcbf

velocity:
  n_kappa_particles: 4
